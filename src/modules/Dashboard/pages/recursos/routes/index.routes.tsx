import { RouteObject } from "react-router-dom";
import Recursos from "../page";

const recursosPage: RouteObject[] = [
  {
    path: "/recursos",
    element: <Recursos />,
  },
];

export { recursosPage };
